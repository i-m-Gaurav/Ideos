export interface Project {
  title: string;
  category: string;
  description: string;
  techStack: string;
  requirements: string;
  timeline: string;
  teamSize: string;
  repoUrl: string;
  demoUrl: string | null;
}
